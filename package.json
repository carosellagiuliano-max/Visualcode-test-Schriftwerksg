{
  "name": "schnittwerk-your-style",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npm --prefix frontend run build && npm --prefix backend run start",
    "build": "npm --prefix frontend run build",
    "start:dev": "npm --prefix backend run dev"
  }
}
